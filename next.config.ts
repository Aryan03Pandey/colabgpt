import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.media-allrecipes.com'], // Add the external domain here
  },
};

export default nextConfig;
